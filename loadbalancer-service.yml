apiVersion: v1
kind: Service
metadata:
    name: app-lb-svc
spec:
    ports:
        -   name: http
            port: 3000
            targetPort: 3000
            protocol: TCP
    selector:
        app: app-pod
    type: LoadBalancer